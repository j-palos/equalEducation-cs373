dispatch:
# Default service serves the typical web resources and all static resources.
- url: "*/favicon.ico"
  service: default

# Default service serves simple hostname request.
- url: "www.equaleducation.info/*"
  service: default

- url: "cs373-idb-218121.appspot.com/*"
  service: default
